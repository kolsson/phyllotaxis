// ----------------------------------------------------------------------------
// EXPERIMENTING
// ----------------------------------------------------------------------------

import { irrCircle, irrLine } from "./helpers/irregular";

let testCircle = [];
let testLine1 = [];

export function computeExperimenting() {
  // a test fancyline
  // primLines.push(
  //   new FancyLine({
  //     type: "bezier",
  //     sp: { x: -200, y: -200 },
  //     ep: { x: 200, y: -200 },
  //     index: -1,
  //     stroke: [92, 92, 92],
  //     strokeWeight: mstLineStrokeWeight,
  //     bezierSwing: 200,
  //     showArrows: true,
  //     arrowDistance: params.mstLineArrowDist,
  //     arrowWidth: params.mstLineArrowWidth,
  //     arrowHeight: params.mstLineArrowHeight,
  //     arrowStroke: mstLineArrowStroke,
  //     arrowInterp: mstLineArrowInterp,
  //   })
  // );
  //
  // testCircle = irrCircle(-100, -100, 20);
  //
  // testLine1 = irrLine(-100, -100, 100, 100);
}

export function drawExperimenting() {
  // push();
  // // noiseDetail(2, 0.5);
  // stroke(255, 0, 0);
  // beginShape();
  // testCircle.forEach((p) => curveVertex(p.x, p.y));
  // endShape(CLOSE);
  // stroke(0, 0, 255, 128);
  // circle(-100, -100, 40);
  // pop();
  //
  // push();
  // stroke(255, 0, 0);
  // beginShape();
  // testLine1.forEach((p, i, arr) => {
  //   // if (i === 0 || i === arr.length - 1) curveVertex(p.x, p.y);
  //   curveVertex(p.x, p.y);
  // });
  // endShape();
  // stroke(0, 0, 255, 128);
  // line(-100, -100, 100, 100);
  // circle(-100, -100, 6);
  // circle(100, 100, 6);
  // pop();
}
