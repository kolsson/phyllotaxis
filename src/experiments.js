// ----------------------------------------------------------------------------
// EXPERIMENTING
// ----------------------------------------------------------------------------

import { irrCircle } from "./helpers/irregular";

let testCircle = [];

export function computeExperimenting() {
  // a test fancyline
  // primLines.push(
  //   new FancyLine({
  //     type: "bezier",
  //     sp: { x: -200, y: -200 },
  //     ep: { x: 200, y: -200 },
  //     index: -1,
  //     stroke: [92, 92, 92],
  //     strokeWeight: primStrokeWeight,
  //     bezierSwing: 200,
  //     showArrows: true,
  //     arrowDistance: params.primMstArrowDist,
  //     arrowWidth: params.primMstArrowWidth,
  //     arrowHeight: params.primMstArrowHeight,
  //     arrowStroke: primArrowStroke,
  //     arrowInterp: primArrowInterp,
  //   })
  // );
  // testCircle = irrCircle(-100, -100, 20);
}

export function drawExperimenting() {
  // push();
  // // noiseDetail(2, 0.5);
  // stroke(255, 0, 0);
  // beginShape();
  // testCircle.forEach((p) => {
  //   curveVertex(p.x, p.y);
  // });
  // endShape(CLOSE);
  // stroke(0, 0, 255, 128);
  // circle(-100, -100, 40);
  // pop();
}
